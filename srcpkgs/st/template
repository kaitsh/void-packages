# Template file for 'st'
pkgname=st
<<<<<<< HEAD
version=20190130.git
revision=3
=======
version=0.8.1.git
revision=1
>>>>>>> f08414a84c... st: add patches and colortheme
build_style=gnu-makefile
make_use_env=compliant
hostmakedepends="pkg-config"
makedepends="fontconfig-devel libX11-devel libXft-devel git"
depends="ncurses st-terminfo-${version}_${revision}"
short_desc="Simple terminal implementation for X"
maintainer='Juan RP <xtraeme@voidlinux.org>'
license="MIT"
homepage="https://st.suckless.org"
patch_args="-Np1"

do_fetch(){
  git clone https://git.suckless.org/st ${wrksrc}
}

pre_build() {
	#sed -i 's|Liberation Mono|Monospace|g' config.def.h
	[ -e ${FILESDIR}/config.h ] && cp ${FILESDIR}/config.h config.h
	# We will use tic after install. See INSTALL.
	sed -i '/tic/d' Makefile
}

post_install() {
	vinstall st.info 644 usr/share/terminfo/s st.terminfo
	vdoc README
	vlicense LICENSE
}

st-terminfo_package() {
	short_desc+=" - terminfo data"
	noarch=yes
	pkg_install() {
		vmove usr/share/terminfo
	}
}
